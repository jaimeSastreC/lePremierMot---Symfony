{% extends "@App/baseAdmin.html.twig" %}



{% block contenu %}

    <div>
        {{ form_start(formsalle, {'attr': {'class': 'form'}}) }}
        <p>
            {#retourne message erreur si besoin après méthode isValid dans Controleur #}
            {{ form_errors(formsalle.nomSalle) }}
            {{ form_label(formsalle.nomSalle, null, {'label_attr':  {'class': 'form-label'}}) }}
        </p>
        <p>
            {{ form_widget(formsalle.nomSalle, {'attr': {'class': 'form-control'}}) }}
        </p>

        <p>
            {#retourne message erreur si besoin après méthode isValid dans Controleur #}
            {{ form_errors(formsalle.villeSalle) }}
            {{ form_label(formsalle.villeSalle, null, {'label_attr':  {'class': 'form-label'}}) }}
        </p>
        <p>
            {{ form_widget(formsalle.villeSalle, {'attr': {'class': 'form-control'}}) }}
        </p>

        <p>
            {#retourne message erreur si besoin après méthode isValid dans Controleur #}
            {{ form_errors(formsalle.placesSalle) }}
            {{ form_label(formsalle.placesSalle, null, {'label_attr':  {'class': 'form-label'}}) }}
        </p>
        <p>
            {{ form_widget(formsalle.placesSalle, {'attr': {'class': 'form-control'}}) }}
        </p>

        <p>
            {{ form_widget(formsalle.save, {'attr': {'class': 'btn btn-primary'}}) }}
        </p>

        {# génération du champ CSRF - _token#  (Cross Site Request Forgeries en champ caché #}
        {{ form_rest(formsalle) }}

        {{ form_end(formsalle) }}
    </div>

{% endblock %}
